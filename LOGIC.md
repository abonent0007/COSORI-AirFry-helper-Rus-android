# LOGIC.md — как работает логика Cosory

Документ для разработчика/агента, который продолжит проект. Здесь описано, как из выбранных продуктов и веса получается готовый план: кнопки, температура, время, фазы, напоминания и пояснение «почему такое время».

---

## 1. Поток экранов

```
Welcome ──(чекбокс «прочитал»)──► Calculator ──(Рассчитать)──► Result ──(Начать готовку)──► Preheat ──► Timer
   │
   └──► Reference (открывается с Calculator и из любого места)
```

> Preheat показывается только если план содержит преднагрев; иначе Result ведёт сразу в Timer.

1. **Welcome** — рекомендации по эксплуатации (из `reference.json`). Кнопка «Продолжить» активна только после чекбокса. Флаг сохраняется в `Prefs.welcomeRead`.
2. **Calculator** — три шага: продукты → вес (шаг 50 г, 50–3000 г) → «желаемый результат» (чипы `FinalLook`).
3. **Result** — «Приготовление ~X мин» (без прогрева), отдельная строка прогрева, фазы, карточка «Почему такое время», оценка по продуктам (самый долгий помечен «← ориентир»), предупреждения, советы.
4. **Preheat** — предупреждение перед готовкой: [PREHEAT] 205 °C, 4 мин, встроенный отсчёт с кольцом прогресса.
5. **Timer** — отсчёт по фазам с **кольцом прогресса**, привязанным к времени (полное → пустое, к концу краснеет), старт/пауза/сброс, вибрация.
6. **Reference** — вкладки: Кнопки · Первые шаги · Безопасность · Уход · Неисправности · Советы. Во вкладке «Уход» — вход в «Паровую очистку».

Состояние держит `CalculatorViewModel` (`StateFlow<CalculatorUiState>`); план строится по кнопке «Рассчитать».

---

## 2. Данные

Все данные — в `app/src/main/assets/data/` (DTO в `data/dto/Dtos.kt`, маппинг в `CookingRepository`).

### products.json (53 продукта)

| Поле | Смысл |
|------|-------|
| `id` | Уникальный идентификатор |
| `nameRu` | Название на русском |
| `group` | Имя константы `ProductGroup` (`POULTRY`, `MEAT`, `FISH`, `POTATO`, `VEGETABLES`, `MUSHROOMS`, `DOUGH`, `DESSERTS`, `EGGS`, `FROZEN`) |
| `mode` | Имя константы `CookingMode` (`AIR_FRY`, `ROAST`, `BAKE`, `GRILL`, `FROZEN`, `REHEAT`, `DEHYDRATE`, `PROOF`) |
| `baseWeightG` | Вес, для которого указано базовое время |
| `baseTimeMin`, `baseTempC` | Базовое время и температура |
| `timeExponent` | Коэффициент роста времени от веса (см. §3) |
| `turnOver` / `shake` | Перевернуть / встряхнуть на половине фазы |
| `cheeseAtEndMin` | За сколько минут до конца добавить сыр (0 = не нужно) |
| `canBrowning` | Можно ли финишировать в [GRILL] для корочки |
| `tips` | Подсказки, показываются в плане |

**Времена и температуры калиброваны под турбо-конвекцию CAF-DC601** (официальный Cosori Cooking Chart, а не духовка): аэрогриль готовит в 1,5–2 раза быстрее духовки. При правках базовых значений — прогонять kotlinc-харнесс и сверять температуру с диапазоном режима.

### modes.json (10 режимов — официальная таблица из мануала CAF-DC601-KEU)

| Режим | На корпусе | Дефолт | Диапазон | Время | Обдув |
|---|---|---|---|---|---|
| AIR_FRY | AIR FRY | 195 °C / 10 мин | 150–230 °C | 1–60 мин | 5 |
| ROAST | ROAST | 220 °C / 12 мин | 175–230 °C | 1–60 мин | 5 |
| BAKE | BAKE | 165 °C / 20 мин | 125–205 °C | 1–60 мин | 3 |
| GRILL | GRILL | 230 °C / 5 мин | **220–230 °C** | 1–30 мин | 5 |
| FROZEN | FROZEN | 200 °C / 14 мин | 150–230 °C | 1–30 мин | 5 |
| REHEAT | REHEAT | 200 °C / 5 мин | 150–220 °C | 1–60 мин | 4 |
| DEHYDRATE | **DRY** | 55 °C / 6 ч | 35–95 °C | 15 мин–24 ч | 1 |
| PROOF | PROOF | 30 °C / 1 ч | 30–45 °C | 15 мин–12 ч | 1 |
| PREHEAT | PREHEAT | 205 °C / 4 мин | 75–230 °C | авто | 4 |
| KEEP_WARM | **WARM** | 75 °C / 30 мин | 60–90 °C | 1–60 мин | 2 |

> **Важно:** на панели KEU подписи **DRY** и **WARM** (сокращения). BROIL — это US-модель, у KEU его нет. Shake — индикатор дисплея умных моделей, на KEU отсутствует. `labelEn` в `CookingMode` обязан совпадать с надписью на корпусе.

### reference.json

Тексты справочника: `welcome` (4 секции), `buttons` (10 кнопок как на корпусе), `firstSteps`, `safety`, `care`, `steamClean`, `troubleshooting`, `errorCodes` (E01/E21, E02/E22, E12), `tips`.

---

## 3. Ядро: `CookingCalculator`

Вход:

```kotlin
CookingRequest(items = listOf(CookingItem(product, weightG)), looks = setOf(FinalLook))
```

Выход — `CookingPlan(preheat, phases, totalTimeMin, cookingTimeMin, perProduct, warnings, tips, rationale)`.

### Шаг 1. Коррекция продукта (`adjust`)

```
time = baseTimeMin × (weight / baseWeightG) ^ timeExponent
```

- Степенная зависимость: время растёт **медленнее** веса (воздух обтекает куски). `timeExponent` 0.1–0.25 — мелочь, 0.3–0.4 — куски и овощи, 0.4 — крупные куски.
- Вес зажимается 50…3000 г, время — 3…90 мин.
- Модификаторы look-ов применяются как **максимум**, а не произведение, и общий фактор ограничен **×1.15** (см. §4).
- Температура зажимается в официальный диапазон режима (`clampTemp`).

### Шаг 2. Кластеризация (`cluster`)

Продукты сортируются по времени (убывание). Продукт попадает в кластер, если:

- **одно семейство режимов**: `CRISP` — Air Fry, Grill, Frozen; `OVEN` — Bake, Roast, Reheat, Preheat; `DRY` — Dry; `PROOF`; `WARM`;
- **разница температур ≤ 15 °C**.

Один кластер = одна фаза: всё кладётся в корзину одновременно.

### Шаг 3. Фазы готовки (`buildCookPhase`)

- Время фазы = `max(время в кластере) × loadFactor(общий вес)`.
- `loadFactor = 1 + 0.10 × (вес − 500 г) / 1000 г`, максимум 1.25. Турбо-обдув справляется с загрузкой лучше духовки, поэтому надбавка мягкая.
- Режим и температура — у «якоря» (самого долгого продукта кластера).
- Напоминания: переворот/встряска на половине; «Добавить позже» для продуктов короче 60 % времени фазы; сыр за N минут; **«Проверьте готовность» за 2 минуты до конца** (аэрогриль быстрый).

### Шаг 4. Фаза подрумянивания (`buildBrowningPhase`)

Если выбран `GOLDEN_CRUST`/`CRISPY` и есть продукты с `canBrowning = true` — добавляется последняя фаза [GRILL] **230 °C** (диапазон режима 220–230 °C), 2 мин (хрустящая — 3 мин). Для замороженных вместо Grill-фазы +1 минута к готовке.

### Шаг 5. Преднагрев

Если есть режимы Air Fry / Roast / Bake / Grill / Frozen — шаг [PREHEAT] 205 °C, 4 мин. `totalTimeMin = 4 + сумма фаз`, а `cookingTimeMin` = только фазы (в UI это «Приготовление ~X мин» + отдельная строка прогрева — чтобы не выставить таймер на сумму).

### Шаг 6. Пояснение (`rationale`) — карточка «Почему такое время»

Генерируется в домене, отображается с чипами кнопок:

1. «Обычно это блюдо/такая комбинация готовится ~X мин (без прогрева).»
2. «Ориентир — самый долгий продукт: «…» ~N мин.» (если продуктов >1)
3. «Поправка на загрузку корзины (… г): +N мин.» (если применена)
4. ««…» добавите позже — ему нужно ~N мин.»
5. «Готовим в несколько приёмов: сначала [MODE], затем [MODE].»
6. «Диапазон режима [MODE]: 150–230 °C, 1–60 мин.»
7. «Финал: [GRILL] 230 °C, 2 мин — румяная корочка.»

---

## 4. Модификаторы «желаемого результата»

Применяются к каждому продукту до кластеризации. **Не перемножаются** — берётся максимум по времени, общий лимит ×1.15; температура снижается и зажимается в диапазон режима.

| Look | Температура | Время | Дополнительно |
|------|-------------|-------|----------------|
| Румяная корочка | — | — | фаза [GRILL] 230 °C / 2 мин |
| Хрустящая корочка | — | +1 мин (для Frozen) | фаза [GRILL] 230 °C / 3 мин |
| Сочный внутри | −5 °C | ×1.05 | — |
| Глубокая пропекаемость | — | ×1.10 | — |
| Мягкий, нежный | −10 °C | ×1.10 | — |
| Расплавленный сыр | — | — | напоминание про сыр за 3 мин |

---

## 5. Примеры

**Сосиски 400 г** → [AIR_FRY] 210 °C, 9 мин; плюс прогрев 4 мин. Карточка: «Обычно это блюдо готовится ~9 мин», диапазон режима Air Fry.

**Куриное филе 200 г + картофель дольками 500 г** → один кластер: max(15; 16) × 1.02 = 16 мин при 200 °C. Напоминания: перевернуть филе и встряхнуть картофель на 8-й мин. Ориентир — картофель.

**Брауни 400 г (Bake 160 °C, 15 мин) + наггетсы 400 г (Frozen 195 °C, 10 мин)** → две фазы: Bake 15 мин, затем Frozen 10 мин; «Готовим в несколько приёмов».

**Филе 200 г + «румяная корочка»** → [AIR_FRY] 195 °C 15 мин → [GRILL] 230 °C 2 мин. «Финал: Grill 230 °C, 2 мин».

**Лосось 250 г (9 мин) + картофель 500 г (16 мин)** → один кластер 16 мин; «Лосось добавите позже — ему нужно ~9 мин».

---

## 6. Константы (`CookingRules`)

| Константа | Значение | Смысл |
|-----------|----------|-------|
| `TEMP_TOLERANCE` | 15 °C | Допуск совместимости температур |
| `ADD_LATER_RATIO` | 0.6 | Порог «добавить позже» |
| `LOAD_EXTRA_PER_KG` | 0.10 | Надбавка времени за лишний кг |
| `MAX_LOAD_FACTOR` | 1.25 | Потолок надбавки |
| `MAX_TIME_FACTOR` | 1.15 | Потолок модификаторов look |
| `COMFORT_WEIGHT_G` | 1500 | Порог предупреждения о весе |
| `PREHEAT_TEMP_C / TIME_MIN` | 205 / 4 | Преднагрев |
| `BROWNING_TEMP_C / TIME` | 230 / 2–3 | Фаза корочки |
| `CHECK_EARLY_MIN` | 2 | «Проверьте за 2 мин до конца» |
| `MIN/MAX_TIME_MIN` | 3 / 90 | Зажим времени фазы |

---

## 7. Ограничения и допущения

- Обдув не регулируется вручную — приложение показывает справочный уровень и не даёт его менять.
- Формулы — калибруемая эвристика: правьте `baseTimeMin`/`baseTempC`/`timeExponent` в `products.json`; код не нужен. Температура обязана попадать в диапазон режима из `modes.json`.
- Комбинации look-ов ограничены +15 % по времени — специально, чтобы «сочный + нежный + глубокая пропекаемость» не давали скачок времени.
- План статический: пересчитывается по кнопке «Рассчитать».

## 8. Паровая очистка камеры (неофициальная методика)

Экран `ui/steamclean/SteamCleanScreen.kt`, данные — `reference.json → steamClean`, вход — вкладка «Уход» справочника.

**Физика:** 1 мл воды → ~1,7 л пара; 100–150 мл заполняют камеру многократно. Температура пара ≤ ~100 °C, поэтому 160 °C нужны для кипения и циркуляции. На нагрев и испарение 100 мл ≈ 260 кДж. Пар размягчает жир, при остывании конденсируется на решётке нагревателя.

**Параметры:** вода 100–150 мл в жаростойкой керамике (не в корзину!), [AIR_FRY] 160 °C, 12 мин, выдержка 8 мин закрытым, затем протереть.

**Таймер:** двухфазный (пропаривание → выдержка) с кольцом прогресса, вибрация по завершении.

**Ограничения:** не из официальной инструкции Cosori; вода — только в отдельной чашке; не запускать с едой.

## 9. UI-слой: движение и свечение

- Токены движения — `ui/theme/Motion.kt` (280/200 мс переходы, stagger 40 мс, пульс 1600 мс, ease-out; гейт `animationsEnabled()` — reduced motion).
- Переходы страниц — `NavHost` enter/exit/popEnter/popExit (slide 25 % + fade).
- Свечение — `ui/components/Glow.kt`: `ctaGlow`, `pulseGlow`, `pressScale`, `glowTextStyle`, `GlowButton`.
- Кольцо таймера — `ui/components/TimerRing.kt`: доля оставшегося времени, свечение, к концу цвет уходит в красный.
- Напоминания на экране таймера формулируются **под обратный отсчёт**: «Когда на таймере 08:00 — Перевернуть…» (в домене `Reminder.atMin` — это минута от старта, UI конвертирует в остаток). На экране плана те же напоминания показаны как «На N-й мин» (план готовки).
- Таймер и Прогрев: контент прокручивается (`verticalScroll`, без ползунка), кнопки управления закреплены снизу и видны на любом экране.
- Звук завершения — `playTimerBell()` (`ui/components/TimerUtils.kt`): MediaPlayer на потоке `USAGE_ALARM` (ресурс `res/raw/timer_bell.mp3`), вызывается вместе с `vibrate()` в Timer, Preheat и Steam Clean.
- Правило: 1–2 анимированных элемента на экран; в списках — никаких бесконечных анимаций.

## 10. Что осталось следующему агенту

- Уведомления таймера через AlarmManager/foreground-сервис (сейчас отсчёт живёт при открытом экране).
- По желанию: Room вместо JSON, избранные рецепты, профили, фото блюд.
- Проверять правки домена kotlinc-харнессом и JUnit-тестами (`app/src/test/java/com/cosory/app/domain/CookingCalculatorTest.kt` — 15 сценариев).
